# RCP image dependencies.
# PyTorch is intentionally not listed: the RCP tutorial uses an NVIDIA PyTorch
# base image, and installing another torch wheel here can silently mismatch CUDA.
matplotlib==3.11.0
neural_ssm==0.4
numpy==2.5.0
streamlit==1.58.0
